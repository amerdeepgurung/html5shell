var amConfigCourse = {
        isLMSRequired : false,
        isTracked : true,
        deviceType : 'none'
    };

console.log(amConfigCourse.isLMSRequired);
console.log(amConfigCourse.isTracked);
console.log(amConfigCourse.deviceType);